import React from 'react';
import { CustomControlProps } from '../common/types';
declare const CustomControl: {
    ({ bindingPosition, children, }: CustomControlProps): React.ReactPortal | null;
    displayName: string;
};
export default CustomControl;
