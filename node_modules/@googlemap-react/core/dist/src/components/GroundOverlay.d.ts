import { GroundOverlayProps } from '../common/types';
declare const GroundOverlay: {
    ({ id, opts, onClick, onDoubleClick, }: GroundOverlayProps): null;
    displayName: string;
};
export default GroundOverlay;
